1. select countries.name,language,percentage from languages join countries on countries.id = languages.country_id where language = "Slovene" order by percentage desc;

2. select countries.name, count(cities.id) as num_cities from cities join countries on countries.id = cities.country_id group by countries.id order by num_cities desc;

3. 
